<?xml version="1.0" encoding="UTF-8"?>
<!-- $Id: build.xml,v 1.2 2011/07/25 16:18:16 headley Exp $ -->
<project name="foce" basedir=".">
	
    <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Load property files in this order:

    1. User properties
    2. This level properties

    This allows a user to have specific settings that override any other
    settings (other than command-line, which always take precedence).
    -->
    <property file="${user.home}/build.properties" />
    <property file="build.properties" />

	<!-- show FOCE2011 help  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
	
	<target name="help">
	<local name="pname"/>
    <property name="pname" value="foce" />
		<echo>
		${pname}.native        - native code
		</echo>
	</target>
	
	<!-- Build native FOCE code ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
	
		<target name="native">
		<echo>Building FOCE native code (continue=${foce.contflag})</echo>
		<exec executable="${foce.make}" >
		<arg line="${foce.contflag} -C ${foce.dir}" />
		</exec>
		</target>
		
	
	</project>